import math
print(math.ceil(2.1))
print(math.floor(2.1))
print(math.sqrt(4))   #제곱근

import function #import 뒤에 가져올 파일등을 쓰면 그 파일을 가져온다
from function import a3  # function파일의 일부만을 가져오고 싶으면 from을 사용한다
print(a3())         # !!!일부만을 가져오고 싶었는데 실패...
